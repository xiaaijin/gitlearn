import { mapState, mapActions } from 'vuex'

const mixin = {
    data () {
        return {}
    },
    computed: {
        ...mapState({})
    },

    methods: {
        ...mapActions([])
    }
}

export default mixin
